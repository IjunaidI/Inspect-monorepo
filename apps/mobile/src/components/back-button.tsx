/**
 * @deprecated INS-095 — `BackButton` lives in the kit (`@/components/ui`). This
 * shim keeps the 20 pre-kit import sites compiling until INS-096 re-points them.
 */
export { BackButton } from './ui/header';
