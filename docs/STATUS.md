# Project Status — Inspect

> **Last verified: 2026-06-20.** Source-of-truth dashboard. Keep it current after every session
> (see [CLAUDE.md → Documentation workflow](../CLAUDE.md)). Backlog: [future/BACKLOG.md](future/BACKLOG.md).
> Product: a tamper-proof, AQL-driven pre-shipment QC inspection platform for textiles/garments
> (multi-tenant B2B SaaS; web-first MVP, mobile deferred). Requirements: [done/specs/2026-06-06-inspect-mvp-requirements-design.md](done/specs/2026-06-06-inspect-mvp-requirements-design.md).

## Tests
- **API (Jest): 97 passing** across 12 suites — all pure-unit, **no DB**. (AQL ~39, auth 25, tamper-proof 14, audit-chain 7, storage/sigv4 5, inspection-mapping ~5, app 1.)
- **Integration / e2e: effectively none** — 1 trivial `test/app.e2e-spec.ts`. Nothing exercises Prisma/guards/RBAC/lifecycle against a real database (INS-009).
- **Web: no unit-test runner.** Console verified by `tsc` + `next build` only.

## Maturity legend
`production-ready` · `working-with-gaps` · `built-unverified` (compiles + type-checks, **never run against a real DB**) · `planned`

## Where each pillar stands

| Pillar | Maturity | State (one line) | Governing doc | Open backlog |
|---|---|---|---|---|
| AQL domain core | production-ready | Pure ISO 2859-1 / Z1.4 single-sampling Level II engine, fully unit-tested (~39 cases); verified band G–N at AQL {1.0,1.5,2.5,4.0,6.5} + critical Ac0; no DB. | [done/plans/2026-06-06-inspect-phase1-foundation-domain-core.md](done/plans/2026-06-06-inspect-phase1-foundation-domain-core.md) | — |
| Data model & schema | working-with-gaps | 25-model orgId-scoped Prisma schema; `00000000000000_init` **applied to Postgres 16 — all 25 tables created** (2026-06-20); 14-defect global seed loaded. 7 cross-cutting invariants still **app-layer-only** (no DB enforcement). | [reference/inspect-schema.md](reference/inspect-schema.md) | INS-010 |
| Tamper-proof & audit | working-with-gaps | canonicalize/content-hash/Ed25519 + audit-chain helper unit-tested (14 + 7); but `audit.service` untested, `audit.append` wired into only **2 of ~15** mutating services, append-only + monotonic sequence are caller-discipline guards (no DB triggers). | [done/plans/2026-06-06-inspect-phase1-foundation-domain-core.md](done/plans/2026-06-06-inspect-phase1-foundation-domain-core.md) | INS-006, INS-011, INS-012, INS-013 |
| Auth & RBAC | working-with-gaps | JWT(HS256)+scrypt+additive RBAC libs unit-tested (25); **login → JWT, `/auth/me`, the JwtAuthGuard + tenant-scope guard now verified live** against the server DB (2026-06-20). Token refresh + the full RBAC matrix (401/403/cross-org) still untested. | [in-progress/plans/2026-06-06-inspect-phase2-auth-tenancy.md](in-progress/plans/2026-06-06-inspect-phase2-auth-tenancy.md) | INS-009 |
| Tenancy & onboarding | built-unverified | Invite-only orgs/users/buyer-guest provisioning controllers+services exist (untested, DB-unverified) and **never email** the magic-link token (returned to the caller only). | [in-progress/plans/2026-06-06-inspect-phase2-auth-tenancy.md](in-progress/plans/2026-06-06-inspect-phase2-auth-tenancy.md) | INS-004, INS-029 |
| Workspace CRUD | built-unverified | Buyers/Suppliers/POs/Products CRUD controllers+services exist but have no specs, **write no audit entries**, expose **no counts/aggregation**, and are DB-unverified. | [done/specs/2026-06-06-inspect-mvp-requirements-design.md](done/specs/2026-06-06-inspect-mvp-requirements-design.md) | INS-005, INS-034 |
| Loop presets | built-unverified | Versioned loop-preset builder controller+service exist (preset step/field/allowed-defect models) but untested/unverified; the web builder is fully static (no persistence). | [done/specs/2026-06-06-inspect-mvp-requirements-design.md](done/specs/2026-06-06-inspect-mvp-requirements-design.md) | INS-024 |
| Inspection lifecycle | built-unverified | `inspections.service` does create(snapshot)/submit(evaluate→AqlResult+BillableEvent)/decision with status guards + idempotency; only pure mapping helpers tested; service untested, DB-unverified; immutability is app-layer status sets only. | [done/specs/2026-06-06-inspect-mvp-requirements-design.md](done/specs/2026-06-06-inspect-mvp-requirements-design.md) | INS-014, INS-018, INS-021 |
| Populate console | built-unverified | `populate.service` enforces LOCKED immutability, catalog-XOR-custom and `clientRequestId` idempotency but has **no spec**; SigV4 presign lib tested; the entire web populate screen is static (no upload/tag/measure/submit). | [done/specs/2026-06-06-inspect-mvp-requirements-design.md](done/specs/2026-06-06-inspect-mvp-requirements-design.md) | INS-007, INS-016, INS-023 |
| Defect catalog | built-unverified | Hybrid global(14 seeded)+per-org+free-text catalog controller+service; seed idempotent; controller untested; catalog-XOR-custom enforced app-side only. | [done/specs/2026-06-06-inspect-mvp-requirements-design.md](done/specs/2026-06-06-inspect-mvp-requirements-design.md) | INS-015 |
| Reports & verification | working-with-gaps | `reports.service` builds a signed report record (canonicalSnapshot + Ed25519 + audit.append) but **never renders the PDF binary** (`pdfStorageKey` unset, no `pdf-lib`); no public verify page UI; service untested; delivery records modeled but **nothing sends**. | [done/specs/2026-06-06-inspect-mvp-requirements-design.md](done/specs/2026-06-06-inspect-mvp-requirements-design.md) | INS-003, INS-017, INS-019, INS-020, INS-033 |
| Guest portal | built-unverified | Magic-link guest controller+service exist (untested); the web `/portal` is fully static — hardcoded reports, no tokenized auth, no buyer-scoped fetch, no real PDF download. | [done/specs/2026-06-06-inspect-mvp-requirements-design.md](done/specs/2026-06-06-inspect-mvp-requirements-design.md) | INS-025 |
| Web console | working-with-gaps | Next.js 15 11-screen console renders design-faithfully; authenticated `apiPost/Put/Patch/Delete` helpers now exist (INS-022, type-checked; live smoke pending a DB), but the screens aren't wired to them yet — only Login performs a live write; 3 list screens read live with lossy demo-fallback; ~8 screens are static placeholders; shell user hardcoded (no session/sign-out). | [done/specs/2026-06-06-inspect-mvp-requirements-design.md](done/specs/2026-06-06-inspect-mvp-requirements-design.md) | INS-022, INS-026, INS-027, INS-028, INS-030, INS-031, INS-032 |
| Infra & CI | working-with-gaps | Stack now **boots end-to-end against the Railway managed Postgres+Redis** (2026-06-20, `/health` reports both up); `.env.example` scrubbed (INS-002, live-cred rotation pending). Still: no integration/CI suite against a DB (INS-009), `@inspect/shared-types` built but **unlinked** (INS-008), object storage (MinIO) not yet wired. | [reference/inspect-build-index.md](reference/inspect-build-index.md) | INS-002, INS-008, INS-009 |

## Active work
- **INS-001 + INS-002 — in progress (started 2026-06-20).** DB-free readiness landed: `.env.example` scrubbed to placeholders, a local `.env` + a fresh Ed25519 signing key generated, and `prisma validate` + `prisma generate` are green (the 25-model schema is structurally sound, the client builds). Runbook + findings: [in-progress/plans/2026-06-20-ins-001-stand-up-and-verify.md](in-progress/plans/2026-06-20-ins-001-stand-up-and-verify.md).
- **DB-free prep also landed (2026-06-20):** the web write helper `apiPost/Put/Patch/Delete` + `ApiError` ([INS-022](future/BACKLOG.md), type-checked) and the first-Platform-Admin bootstrap in `prisma/seed.ts` (Task 0 — env-driven, idempotent) — so once a DB exists the stand-up is migrate → seed (admin included) → log in → drive the loop.
- **✅ Stack runs end-to-end (2026-06-20).** Wired `.env` to the **Railway managed Postgres + Redis** (public TCP proxy) instead of local Docker. `prisma migrate deploy` applied the init migration to an empty Postgres 16 (**all 25 tables created**); `db seed` loaded the 14 global defects + bootstrapped the first Platform Admin. The **API boots on :3000** and `GET /health` returns **`database: up` + `redis: up`**; `POST /auth/login` (bootstrap admin) issues a JWT and `GET /auth/me` returns the principal — the auth round-trip (scrypt + JWT + guards) and tenant-scoping (an org-scoped route correctly 403s the no-org admin) are **verified live**. The **web console runs on :3001**.
- **Remaining for INS-001:** drive the full create→submit→AQL→report loop (needs an Org Owner via invite, and the populate photo step needs **MinIO/S3**, not yet up) and add ≥1 CI integration test (INS-009).
- **INS-002 still pending (user-side):** rotate the Railway `POSTGRES_PASSWORD`/`REDIS_PASSWORD` (these are temp creds, but they remain in git **history**; working tree is clean) and decide whether to scrub history.
- **Now unblocked:** every HIGH web-write item (INS-022/023/024/026/027) and the aggregation/audit-on-write work can be developed + verified against a DB that is known to run.

## Map
- Shipped: [done/](done/) · In progress: [in-progress/](in-progress/) · Drafts: [future/](future/) · Living refs: [reference/](reference/) · Backlog: [future/BACKLOG.md](future/BACKLOG.md)
- Historical handoff (superseded by this dashboard): [done/plans/2026-06-07-inspect-status-and-next-steps.md](done/plans/2026-06-07-inspect-status-and-next-steps.md)
